<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xd.smartworksite.review.mapper.ReviewFieldSchemaMapper">
  <resultMap id="SchemaMap" type="com.xd.smartworksite.review.domain.ReviewFieldSchema">
    <id property="id" column="id"/><result property="projectId" column="project_id"/><result property="templateId" column="template_id"/>
    <result property="version" column="version"/><result property="status" column="status"/><result property="createdBy" column="created_by"/>
    <result property="fieldsJson" column="fields_json"/>
  </resultMap>
  <select id="selectActive" resultMap="SchemaMap">select id, project_id, template_id, version, fields_json, status, created_by from review_field_schema where project_id=#{projectId} and template_id=#{templateId} and status='ACTIVE' and deleted=0 order by version desc limit 1</select>
  <select id="selectVersion" resultMap="SchemaMap">select id, project_id, template_id, version, fields_json, status, created_by from review_field_schema where project_id=#{projectId} and template_id=#{templateId} and version=#{version} and deleted=0</select>
  <select id="selectNextVersion" resultType="int">select coalesce(max(version), 0) + 1 from review_field_schema where project_id=#{projectId} and template_id=#{templateId}</select>
  <update id="deactivate">update review_field_schema set status='HISTORY', updated_by=#{updatedBy} where project_id=#{projectId} and template_id=#{templateId} and status='ACTIVE' and deleted=0</update>
  <insert id="insert" useGeneratedKeys="true" keyProperty="id">insert into review_field_schema(project_id,template_id,version,fields_json,status,created_by,updated_by) values(#{projectId},#{templateId},#{version},#{fieldsJson},#{status},#{createdBy},#{createdBy})</insert>
</mapper>
