# 审查、政策与知识库问题实机验收

日期：2026-09-18

## 结论

本次修复已部署到 `xidian@172.18.12.6:/home/xidian/sjw/smart_worksite`，部署提交为 `0cf2c1b83c510e861e98b3a1311ad1def9e4c24c`。合规审查结果恢复与人工确认分层、政策时间语义与栏目重抓、知识库文档分页和筛选均取得真实 API、持久化数据及服务健康证据。

浏览器控制通道仍返回 `unsupported Codex auth method: apikey`，因此本报告不把 API 验收冒充为浏览器交互验收。页面代码已通过前端完整测试和生产构建，视觉点击流程仍需浏览器控制基础设施恢复后补充证据。

## 自动化门禁

- 前端：25 个测试文件、172 项测试通过。
- Java：481 项测试通过，0 失败、0 错误、0 跳过。
- Python 政策爬虫：32 项通过；`test_routes_policy_service_shares_settings` 因 Windows 本机环境缺少 Pillow 而明确排除。政策排序、去重、日期解析和限制测试均通过。
- 前端生产构建：`vue-tsc --noEmit && vite build` 通过。
- `git diff --check`：无内容错误，只有 Windows CRLF 转换提示。
- 独立 Trellis 集成审查发现并在部署前修复了一个迟到恢复响应覆盖用户选择/新提交的竞态；对应测试先失败后通过。

## 部署证据

- 部署前服务器提交：`1eba3672951fbf40bf19eb863ece80918edd2981`。
- 部署后服务器提交：`0cf2c1b83c510e861e98b3a1311ad1def9e4c24c`。
- 服务器不能及时获取 GitHub 新引用，因此使用从已验证提交生成的增量 Git bundle 快进部署；没有执行 reset。
- 保留服务器原有未跟踪文件：`deploy/.env.before-pp-ocrv5`、两个备份配置及 `python-ai-service/models/`。
- Frontend、Backend、Python AI、MySQL、Redis、MinIO 和 MinIO Console 均运行；Backend/Python AI 健康检查为 UP。
- H100 本地模型 chat、vision、embedding、rerank 均为 UP。该证据只证明 H100 功能链路，不代表客户双 A6000 性能验收。

## 合规审查

真实项目 1 的审查记录总数为 114，列表第一页第一条为记录 121，因此没有有效本地偏好时会恢复到该最新服务端记录。

记录 121 的真实数据：

- 状态：`PARTIAL_SUCCESS`。
- 顶层明确问题数：0。
- 规则结果数：1。
- 规则校验错误：`模型判定与问题明细不一致，需人工确认`。

页面现在把顶层问题显示为“明确问题”，把该类没有可处理 `issueId` 的矛盾结果显示为“待人工确认”，并保留原始 JSON。终态记录不轮询；非终态记录恢复轮询；迟到的项目恢复响应不能覆盖用户后续选择或新提交。

## 政策资讯

来源 8 是单篇文章 URL：

`https://sjw.qingdao.gov.cn/cxjsj3/cxjsj46/202608/t20260828_10715324.shtml`

它只能刷新当前文章，不能发现同栏目后续文章。页面通过 URL 日期模式显示该边界提示。

来源 2 是栏目 URL：

`https://sjw.qingdao.gov.cn/cxjsj1/cxjsj35/`

真实重抓任务 893 的结果：

- 状态：`SUCCESS`。
- 抓取：15 条。
- 入库：15 条。
- 失败：0 条。
- 开始：2026-09-18 15:27:19。
- 完成：2026-09-18 15:33:20。

对既有文章 102、103、104、105、117 比较确认：`publishDate` 与 `createdAt` 保持不变，`updatedAt` 更新到本次重抓时间。页面因此使用“发布日期”“首次入库”“记录更新时间”，不把 `updatedAt` 描述为“最近成功抓取”。

日期排序只对 URL 路径中可可靠解析的 `YYYYMMDD`、`YYYY/MM/DD`、`YYYY-MM-DD` 等格式生效；无日期链接保持原 DOM 顺序。这是显式设计边界，不是对所有网站发布时间的推测。

## 知识库分页

真实知识库数据：

- 知识库 9：11 条文档。
- 知识库 5：36 条文档。

知识库 5 以 `pageSize=5` 请求第 1 页和第 2 页，各返回 5 条，两页文档 ID 无重复。`indexStatus=SUCCESS` 返回总数 34 且样本状态均为 `SUCCESS`；关键词“发票”返回总数 1，标题为 `个人发票.png`。

前端现已发送 `pageNo`、`pageSize`、`keyword`、`indexStatus` 并使用服务端 `total`。轮询保留当前页；切库、筛选和上传回到第一页；删除后页为空时回退一页；过期列表和解析状态响应不会覆盖当前上下文。

## 未关闭边界

- 浏览器自动化授权错误阻止了本轮真实页面点击/视觉证据；不能宣称 Chrome E2E 已完成。
- 最近审查记录选择器加载最近 50 条；超过范围的旧本地偏好会回退到服务器最新记录。
- Python 本机完整路由测试需要补装 Pillow 后才能做到零排除，但部署容器已健康且真实政策任务成功。
- 客户双 A6000 性能、显存、并发和长上下文仍需在客户硬件上独立验收。
