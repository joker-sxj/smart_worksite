# 技术设计

正式设计见 `docs/superpowers/specs/2026-09-18-review-policy-knowledge-remediation-design.md`。

本任务分为三个独立工作包：

1. 合规审查用现有记录列表接口恢复服务器状态，并通过纯视图模型区分明确问题与待人工确认规则。
2. 政策页面分别显示来源和系统时间；Python 爬虫在截断候选前按 URL 可解析日期稳定排序。
3. 知识库页面把现有后端分页契约接入统一加载和刷新路径。

不新增数据库迁移。部署以精确 Git 提交为单位，保留前一提交作为回滚点。
